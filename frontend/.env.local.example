# Copy this file to .env.local and adjust if your backend isn't running on
# the default localhost:8000 (for example, when deploying or running the
# backend on a different host/port for the demo).
NEXT_PUBLIC_API_URL=http://localhost:8000
